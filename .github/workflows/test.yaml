name: TEST

on:
  push:
    inputs:
      input:
        description: "Test"
        required: true
        default: "World"

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello ${{ github.event.inputs.input }}
